<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.osidocker.open.micro.mapper.ProductInventoryMapper">

    <update id="updateProductInventory" parameterType="com.osidocker.open.micro.entity.ProductInventory">
        update product_inventory set cnt = #{inventoryCnt} where product_id = #{productId}
    </update>

    <select id="findProductInventory" resultType="com.osidocker.open.micro.entity.ProductInventory">
        SELECT product_id productId,cnt inventoryCnt from product_inventory where product_id = #{productId}
    </select>
</mapper>