<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.osidocker.open.micro.mapper.LoanApplyMapper" >
    <update id="updateLoanApplyStatus">
        UPDATE loan_apply SET STATE = #{status} WHERE apply_id = #{applyId}
    </update>

    <select id="selectApplyId" resultType="java.lang.String">
        select apply_id from system_order where order_id = #{orderId}
    </select>
</mapper>